spring:
  datasource:
    url: jdbc:h2:mem:music_stream_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

deezer:
  base-url: https://api.deezer.com

jamendo:
  base-url: https://api.jamendo.com/v3.0
  client-id:
